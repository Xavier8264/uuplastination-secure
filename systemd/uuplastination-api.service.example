# Copy to /etc/systemd/system/uuplastination-api.service and adjust paths
[Unit]
Description=UU Plastination FastAPI (secure API + WebRTC token)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
# Set to your deployment path
WorkingDirectory=/var/www/secure/uuplastination-secure
Environment=PYTHONUNBUFFERED=1
# Load env file created from .env
EnvironmentFile=/var/www/secure/uuplastination-secure/.env
# Use venv python if available; otherwise set full path
ExecStart=/var/www/secure/uuplastination-secure/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 1
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
